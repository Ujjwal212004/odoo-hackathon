
  # AssetFlow app

  This is a code bundle for AssetFlow app. The original project is available at https://www.figma.com/design/KVl2lbt9fQyzAXZm9LSRwI/AssetFlow-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  